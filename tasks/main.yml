---
# file: git/tasks/main.yml
#
# tasks file
#

- name: include OS family/distribution specific variables
  include_vars: "{{ item }}"
  with_first_found:
    - "../defaults/{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
    - "../defaults/{{ ansible_distribution | lower }}.yml"
    - "../defaults/{{ ansible_os_family | lower }}.yml"

- include: debug.yml
  when: debug | default(false)
  tags: debug

- include: validation.yml
  tags: validation

- name: verify git version
  shell: git --version
  register: test
  changed_when: no
  environment:
    PATH: "{{ git_path }}/bin"

- include: installation.yml
  when: test is defined and test.stdout.find("git version {{ git_version }}") == -1
  tags: installation
